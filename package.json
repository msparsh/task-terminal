{
  "name": "task-terminal",
  "version": "1.0.7",
  "description": "A dedicated pseudo-terminal for Taskbook",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "prebuild": "npm version patch --no-git-tag-version && git add package.json",
    "build": "electron-builder"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.13.3"
  }
}
